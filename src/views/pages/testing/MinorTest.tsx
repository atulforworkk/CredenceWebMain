import React from "react";
import { Button } from "../../../components/button/ButtonCva";

type Props = {};

const MinorTest = (props: Props) => {
  return (
    <div>
      <h1>this is all bout minor testing </h1>
      <Button size="small">About us </Button>
    </div>
  );
};

export default MinorTest;
